import { DynamicUiPipe } from './dynamic-ui.pipe';

describe('DynamicUiPipe', () => {
  it('create an instance', () => {
    const pipe = new DynamicUiPipe();
    expect(pipe).toBeTruthy();
  });
});
